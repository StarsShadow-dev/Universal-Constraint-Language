// compOut
// a string in a struct in a struct

const test = struct {
	const a: 'Bool = true;
	const b = struct {
		const c: 'String = "a string in a struct in a struct";
	};
};

if (test.a) {
	@addTopCodeGen(test.b.c);
}