const js = @import("./js.ucl");
const main = @import("./main.ucl");

const logNumber = fn(n1: Number, n2: Number) {
	js.indent();
	@addCodeGen("console.log(");
	n1;
	@addCodeGen(", ");
	n2;
	@addCodeGen(");\n");
};

const exportTest = fn() {
	logNumber(30, -100);
	logNumber(30, -100);
	logNumber(30, -100);
	logNumber(30, -100);
	main.test();
};