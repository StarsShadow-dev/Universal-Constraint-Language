Maybe = fn(T: Type): Type {
	self = enum {
		none
		some = struct(value: T) {}
	}
	self
}
// @compDebug(Maybe(String))

main = fn(): Maybe(String) {
	// Maybe(String).none
	Maybe(String).some {
		value = "a string!!!"
	}
}
@compDebug(main())