Maybe = fn(T: Type): Type {
	enum {
		none
		some = struct(value: T) {}
	}
}
// @compDebug(Maybe(String))

main = fn(): Maybe(String) {
	// Maybe(String).none
	Maybe(String).some {
		value = "a string!!!"
	}
}
@compDebug(main())